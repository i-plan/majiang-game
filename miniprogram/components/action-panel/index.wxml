<view wx:if="{{actions.length}}" class="action-panel">
  <button
    wx:for="{{actions}}"
    wx:key="index"
    class="action-btn {{disabled ? 'action-btn-disabled' : ''}}"
    data-index="{{index}}"
    bindtap="handleTap"
    disabled="{{disabled}}"
  >
    {{item.label}}
  </button>
</view>
